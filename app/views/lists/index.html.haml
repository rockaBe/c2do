%h1 Listing lists

%table
  %tr
    %th Name
    %th Position
    %th
    %th
    %th

  - @lists.each do |list|
    %tr
      %td.icon-progress-0= list.name
      %td= list.position
      %td= link_to 'Show', list_path(list)
      %td= link_to 'Edit', edit_list_path(list)
      %td= link_to 'Destroy', list_path(list), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New List', new_user_list_path
