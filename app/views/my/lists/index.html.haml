%h1 Listing lists

%table
  %tr
    %th Name
    %th Position
    %th
    %th
    %th

  - @lists.each do |list|
    %tr
      %td= list.name
      %td= list.position
      %td= link_to 'Show', my_list_path(list)
      %td= link_to 'Edit', edit_my_list_path(list)
      %td= link_to 'Destroy', my_list_path(list), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New List', new_my_list_path

