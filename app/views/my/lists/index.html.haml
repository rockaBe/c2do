%h1 Listing lists

%table.large-12
  %tr
    %th{width: "60%"} Name
    %th{width: "13%"}
    %th{width: "13%"}
    %th{width: "13%"}

  - @lists.each do |list|
    %tr
      %td.icon-progress-2= list.name
      %td= link_to t('actions.show'), my_list_path(list), class: 'icon-info-circled'
      %td= link_to t('actions.edit'), edit_my_list_path(list), class: 'icon-edit-circled'
      %td= link_to t('actions.delete'), my_list_path(list), :method => :delete, :data => { :confirm => t('confirmations.deletion', model: t('model.list'), name: list.name) }, class: 'icon-delete-circled'

%br

= link_to t('model.new_list'), new_my_list_path, class: 'button radius'

